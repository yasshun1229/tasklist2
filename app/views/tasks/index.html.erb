<h1>タスク一覧ページ</h1>

<ul>
  <%= @tasks.each do |task| %>
    <li><%= link_to task.id, task %> : task: <%= task.content %> : status:<%= task.status %></li>
  <% end %>
</ul>

<%= link_to "新規タスクの追加", new_task_path %>